package com.example.projectstructure

import android.app.Application

class MainApplication : Application() {
    // init libararies tobe users (firebase,...)
    // create fonts
    // get sharedpreferences  (working through app running...) (global infomation)

}
